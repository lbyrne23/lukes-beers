# Luke's Beer Journey

Interactive stats for Luke Inkster's Untappd check-in history.

Built with React + TypeScript + Vite. Deployed to GitHub Pages.

## Setup

```bash
npm install
npm run dev
```

## Deploy

Push to `main` — GitHub Actions handles the build and deploy automatically.

Make sure GitHub Pages is set to deploy from **GitHub Actions** in repo Settings → Pages.
